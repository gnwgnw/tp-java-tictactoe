<fest:template xmlns:fest="http://fest.mail.ru" context_name='collection'>

    <a href='#' class="button_right">Back</a>

    <fest:each iterate='collection' index='i' value='score'>
        <div>
            <fest:value>score.login</fest:value>
            <fest:value>score.gameCount</fest:value>
        </div>
    </fest:each>

</fest:template>
